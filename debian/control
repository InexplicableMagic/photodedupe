source: photodedupe
Maintainer: kazantsev mikhail <kazan417@mail.ru>
Build-Depends: python3 (>= 3.13),
               lintian (>= 2.122),
               cargo (>=1.85),
               libstd-rust-dev (>=1.85),
               librust-clap-dev (>= 4.5),
               librust-clap-derive-dev (>= 4.5),
               librust-glob-dev (>= 0.3),
               librust-image-dev (>= 0.25),
               librust-indicatif-dev (>= 0.17),
               librust-thiserror-dev (>= 2.0),
               librust-thiserror-impl-dev (>= 2.0),
               librust-threadpool-dev (>= 1.8),
               librust-walkdir-dev (>= 2.5),
               dh-cargo (>= 32),
               debhelper (>= 13.24)
Standards-Version: 4.7.0

Package: photodedupe
Priority: optional
Section: utils
Architecture: amd64
Depends: libc6 (>= 2.34), libgcc-s1 (>= 4.2), ${misc:Depends} 
Homepage: https://github.com/InexplicableMagic/photodedupe
Description: Identifies duplicate photos regardless of scale or file format.
 Photodedupe is a command line utility for identifying duplicate photos 
 regardless of whether the images have been scaled or have differing
 file formats. It compares the image content visually and does not rely
 on metadata or file hashes to perform the de-duplication.
